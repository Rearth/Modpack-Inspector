import { APP_SCALE_STEP, DEFAULT_APP_SCALE, MAX_APP_SCALE, MIN_APP_SCALE, applyAppScale, clampAppScale, getAppScaleFactor } from './settings';

export { APP_SCALE_STEP, DEFAULT_APP_SCALE, MAX_APP_SCALE, MIN_APP_SCALE, applyAppScale, clampAppScale, getAppScaleFactor };